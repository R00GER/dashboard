function DataCardRowItem({ icon }) {
  return (
    <div style={{ height: '50px', marginBottom: '0.5rem' }}>
      <div style={{ marginBottom: '0.5rem' }}>{icon}</div>
    </div>
  );
}

export default DataCardRowItem;
